node_modules/
dist/
.DS_Store

# Probe output (staging observations, regenerated per run). These hold UNSCRUBBED real ids —
# only the recorded fixtures under src/__fixtures__/ are safe to commit.
probe-report.json
surface-probe-report.json

# Never commit local credentials or agent tool state
.env
.env.*
!.env.example
.dev.vars
!.dev.vars.example
.claude/
